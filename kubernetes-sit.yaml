apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend2
  namespace: fintech-sit-carpo-team
  labels:
    app: backend2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend2
  template:
    metadata:
      annotations:
        prometheus.io/path: /actuator/prometheus
        prometheus.io/port: "9090"
        prometheus.io/scrape: "true"
      labels:
        app: backend2
        deploysphere_health: "true"
    spec:
      imagePullSecrets:
        - name: fintech-docker-secret
      containers:
        - name: backend2
          image: carpo-team-docker-registry.fintechchallenge.pl/carpo-team/backend2:latest
          imagePullPolicy: Always
          ports:
            - name: api
              containerPort: 8080
            - name: management
              containerPort: 9090
          resources:
            limits:
              cpu: 400m
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 32Mi
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9090
            initialDelaySeconds: 50
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9090
            initialDelaySeconds: 30
            periodSeconds: 10
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: backend2
  namespace: fintech-sit-carpo-team
  labels:
    app: backend2
    visualize: "true"
    prometheus_monitoring: "true"
spec:
  ports:
    - name: api
      port: 8080
      protocol: TCP
      targetPort: api
  selector:
    app: backend2
---









---
# Note: in a kubernetes secret the string (e.g. generated by htpasswd) must be base64-encoded first.
# To create an encoded user:password pair, the following command can be used:
# htpasswd -nb user password | openssl base64

apiVersion: v1
kind: Secret
metadata:
  name: fintech-sit-carpo-team-basic-auth-secret
  namespace: fintech-sit-carpo-team

data:
  users: |
    YWRtaXNpZTokYXByMSRrTWF0TjR2USRzSmI2bS9TcWQ4TTY2T1F3enFUaGwvCgo=
#encryped username: example, with password: example

---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: fintech-sit-carpo-team-basic-auth-middleware
  namespace: fintech-sit-carpo-team
spec:
  basicAuth:
    secret: fintech-sit-carpo-team-basic-auth-secret
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: backend2
  namespace: fintech-sit-carpo-team
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`backend2.carpo-team.sit.fintechchallenge.pl`)
      middlewares:
        - name: fintech-sit-carpo-team-basic-auth-middleware
          namespace: fintech-sit-carpo-team
      services:
        - kind: Service
          name: backend2
          namespace: fintech-sit-carpo-team
          passHostHeader: true
          port: 8080
          scheme: http
  tls:
    options:
      namespace: fintech-sit-carpo-team
    certResolver: hltech
    domains:
      - main: backend2.carpo-team.sit.fintechchallenge.pl
